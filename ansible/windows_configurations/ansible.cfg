[defaults]
# Shows Ansible task results in YAML format, which makes the output easier to read.
callback_result_format = yaml
# Disables deprecation warnings to keep the terminal output clean.
deprecation_warnings = False
# Hides skipped tasks from the output, because the playbook uses many conditional checks.
display_skipped_hosts = False